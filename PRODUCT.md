# Product

## Register

product

## Users

这个项目面向个人站长和日常高频使用导航页的用户。使用者通常希望在一个页面里快速搜索、分类浏览、跳转到常用网站，也希望通过后台低成本维护分类、站点、背景、一言、页面特效、Live2D 和小工具。

后台的主要用户是站点拥有者本人，场景是反复调整配置、添加站点、创建工具页、部署到 Cloudflare Worker，并让前台保持可浏览、可搜索、可维护。

## Product Purpose

项目的目标是把原本静态的个人导航页升级为可配置的个人门户。成功标准不是展示更多装饰，而是让用户能稳定完成三件事：

- 在前台快速找到并打开目标网站。
- 在后台清楚地管理页面配置、分类、站点和小工具。
- 通过 Worker/KV 把配置和工具页在线保存、在线发布。

## Brand Personality

实用、轻松、可定制。

前台可以保留二次元、动态背景、飘落特效、可爱标签页文案这些轻量个性，但整体仍应服务于导航效率。后台应该更安静、更克制，像配置工作台，不像展示页。

## Anti-references

不要变成泛 AI 风格页面：大面积紫蓝渐变、过度发光、装饰性玻璃拟态、无意义动效、满屏营销式 hero 都不是这个项目的方向。

不要让后台重新堆回一个超长表单页。后台需要分页、分组、状态反馈和清晰的保存流程。

不要再加入会拖慢页面的鼠标拖尾类效果。任何动态效果都必须可关闭、可降级，不应影响搜索、滚动和点击。

不要依赖单一外部资源没有兜底。一言、背景、favicon、Live2D CDN、Worker API 都需要失败处理。

## Design Principles

1. 导航效率优先。前台视觉可以有氛围，但卡片、搜索和分类必须始终清楚。
2. 后台服务任务。后台控件要稳定、统一、可预测，避免装饰性动效和不必要的视觉噪声。
3. 个性可配置。背景、一言、标题、图标、特效、Live2D、小工具都应由配置驱动，而不是写死在页面里。
4. 失败要有兜底。接口不可用、图标获取失败、KV 未绑定、管理员未登录时，都要给出明确反馈。
5. 移动端不是缩小版。手机端应禁用重型装饰，优先保证阅读、点击、滚动和表单编辑。

## Accessibility & Inclusion

目标是尽量接近 WCAG AA：正文和表单文字需要足够对比度，按钮和输入框需要清楚的焦点态，移动端触控目标应接近或超过 44px。

所有页面动效需要支持 `prefers-reduced-motion`。页面特效、标题浮动、卡片 hover、背景视频和 Live2D 都应可关闭或降级。

后台表单需要明确 label、错误提示、保存状态、加载状态和危险操作确认。前台卡片图标必须有兜底，不应因为 favicon 失败导致卡片不可识别。
